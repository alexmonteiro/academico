<%= form_for @telephone_type, :html => { :class => 'form-horizontal' } do |f| %>
  <div class="control-group">
    <%= f.label :telephone_type, :class => 'control-label' %>
    <div class="controls">
      <%= f.text_field :telephone_type, :class => 'text_field' %>
    </div>
  </div>

  <div class="form-actions">
    <%= f.submit nil, :class => 'btn btn-primary' %>
    <%= link_to t('.cancel', :default => t("helpers.links.cancel")),
                telephone_types_path, :class => 'btn' %>
  </div>
<% end %>
