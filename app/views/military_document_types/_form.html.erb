<%= form_for(@military_document_type) do |f| %>
  <% if @military_document_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@military_document_type.errors.count, "error") %> prohibited this military_document_type from being saved:</h2>

      <ul>
      <% @military_document_type.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :description %><br />
    <%= f.text_field :description %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
