<%= form_for(@academic_rule) do |f| %>
  <% if @academic_rule.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@academic_rule.errors.count, "error") %> prohibited this academic_rule from being saved:</h2>

      <ul>
      <% @academic_rule.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :academic_rule_type %><br />
    <%= f.text_field :academic_rule_type_id %>
  </div>
  <div class="field">
    <%= f.label :operator %><br />
    <%= f.text_field :operator %>
  </div>
  <div class="field">
    <%= f.label :value %><br />
    <%= f.number_field :value %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
