<%= semantic_form_for "person" do |form| %>

    <% if !cities.blank? %>
      <%= form.select :city_id, cities.collect{ |c| [c.name,c.id]} %>
    <% else %>
      <%= form.select :city_id, [] %>
    <% end %>

<% end %>