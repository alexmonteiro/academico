<% if !cities.blank? %>
	<%= select_tag 'person[city_id]', options_from_collection_for_select(cities, "id", "name") %>
<% else %>
	<%= select_tag 'person[city_id]', [], :class => 'select_field' %>
<% end %>
