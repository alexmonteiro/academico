<script type="text/javascript">
jQuery(function($) {
  // when the #country field changes
  $("#person_country_id").change(function() {
    // make a POST call and replace the content
    var country = $('select#person_country_id :selected').val();
    var nome_view = $('input#nome_view').val();
    var state = 0
    if(state == "") state="0";
    if(country == "") country="0";
    jQuery.get('/states/update_state_select/' + country,{nome_view: nome_view}, function(data){
        $("#personStates").html(data);
    })
    
    jQuery.get('/cities/update_city_select/' + state, {nome_view:nome_view}, function(data){
        $("#personCities").html(data);
    })
    return false;
  });
  
})
</script>
<%- model_class = Person -%>
<div class="page-header">
  <h1><i class="icon-user"></i> <%=t '.title', :default => t('helpers.titles.new', :model => model_class.model_name.human,
                                :default => "New #{model_class.model_name.human}") %></h1>
</div>
<%= render :partial => 'form' %>
