<%= form_for(@academic_rule_type) do |f| %>
  <% if @academic_rule_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@academic_rule_type.errors.count, "error") %> prohibited this academic_rule_type from being saved:</h2>

      <ul>
      <% @academic_rule_type.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :rule %><br />
    <%= f.text_field :rule %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
