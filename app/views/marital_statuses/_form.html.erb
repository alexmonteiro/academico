<%= form_for(@marital_status) do |f| %>
  <% if @marital_status.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@marital_status.errors.count, "error") %> prohibited this marital_status from being saved:</h2>

      <ul>
      <% @marital_status.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :status %><br />
    <%= f.text_field :status %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
