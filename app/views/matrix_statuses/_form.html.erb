<%= form_for(@matrix_status) do |f| %>
  <% if @matrix_status.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@matrix_status.errors.count, "error") %> prohibited this matrix_status from being saved:</h2>

      <ul>
      <% @matrix_status.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :description %><br />
    <%= f.text_field :description %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
