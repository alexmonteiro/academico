<%= form_for(@education_level) do |f| %>
  <% if @education_level.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@education_level.errors.count, "error") %> prohibited this education_level from being saved:</h2>

      <ul>
      <% @education_level.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :level %><br />
    <%= f.text_field :level %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
