<%= form_for(@street_type) do |f| %>
  <% if @street_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@street_type.errors.count, "error") %> prohibited this street_type from being saved:</h2>

      <ul>
      <% @street_type.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :description %><br />
    <%= f.text_field :description %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
