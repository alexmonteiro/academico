<%= form_for @techaxis_type, :html => { :class => 'form-horizontal' } do |f| %>
  <div class="control-group">
    <%= f.label :techaxis_type, :class => 'control-label' %>
    <div class="controls">
      <%= f.text_field :techaxis_type, :class => 'text_field' %>
    </div>
  </div>

  <div class="form-actions">
    <%= f.submit nil, :class => 'btn btn-primary' %>
    <%= link_to t('.cancel', :default => t("helpers.links.cancel")),
                techaxis_types_path, :class => 'btn' %>
  </div>
<% end %>
