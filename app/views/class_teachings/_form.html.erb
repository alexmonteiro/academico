<%= form_for @class_teaching, :html => { :class => 'form-horizontal' } do |f| %>
  <div class="control-group">
    <%= f.label :discipline_class_id, :class => 'control-label' %>
    <div class="controls">
      <%= f.number_field :discipline_class_id, :class => 'number_field' %>
    </div>
  </div>
  <div class="control-group">
    <%= f.label :person_id, :class => 'control-label' %>
    <div class="controls">
      <%= f.number_field :person_id, :class => 'number_field' %>
    </div>
  </div>
  <div class="control-group">
    <%= f.label :dept_id, :class => 'control-label' %>
    <div class="controls">
      <%= f.number_field :dept_id, :class => 'number_field' %>
    </div>
  </div>

  <div class="form-actions">
    <%= f.submit nil, :class => 'btn btn-primary' %>
    <%= link_to t('.cancel', :default => t("helpers.links.cancel")),
                class_teachings_path, :class => 'btn' %>
  </div>
<% end %>
