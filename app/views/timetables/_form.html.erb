<%= form_for @timetable, :html => { :class => 'form-horizontal' } do |f| %>
  <div class="control-group">
    <%= f.label :matrix_id, :class => 'control-label' %>
    <div class="controls">
      <%= f.number_field :matrix_id, :class => 'number_field' %>
    </div>
  </div>
  <div class="control-group">
    <%= f.label :day_week_id, :class => 'control-label' %>
    <div class="controls">
      <%= f.number_field :day_week_id, :class => 'number_field' %>
    </div>
  </div>

  <div class="form-actions">
    <%= f.submit nil, :class => 'btn btn-primary' %>
    <%= link_to t('.cancel', :default => t("helpers.links.cancel")),
                timetables_path, :class => 'btn' %>
  </div>
<% end %>
