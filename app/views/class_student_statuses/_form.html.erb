<%= form_for(@class_student_status) do |f| %>
  <% if @class_student_status.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@class_student_status.errors.count, "error") %> prohibited this class_student_status from being saved:</h2>

      <ul>
      <% @class_student_status.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :description %><br />
    <%= f.text_field :description %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
