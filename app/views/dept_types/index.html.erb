<h1>Listing dept_types</h1>

<table>
  <tr>
    <th>Dept type</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @dept_types.each do |dept_type| %>
  <tr>
    <td><%= dept_type.dept_type %></td>
    <td><%= link_to 'Show', dept_type %></td>
    <td><%= link_to 'Edit', edit_dept_type_path(dept_type) %></td>
    <td><%= link_to 'Destroy', dept_type, :method => :delete, :data => { :confirm => 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Dept type', new_dept_type_path %>
