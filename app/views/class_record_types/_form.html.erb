<%= form_for(@class_record_type) do |f| %>
  <% if @class_record_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@class_record_type.errors.count, "error") %> prohibited this class_record_type from being saved:</h2>

      <ul>
      <% @class_record_type.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :description %><br />
    <%= f.text_field :description %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
